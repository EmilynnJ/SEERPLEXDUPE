{
  "name": "soulseer",
  "version": "1.0.0",
  "description": "SoulSeer Psychic Reading Platform",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "npm install && prisma generate && cd client && npm install && npm run build",
    "netlify-build": "prisma generate && cd client && npm install && npm run build",
    "client:dev": "cd client && npm run dev",
    "client:build": "cd client && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.4.0",
    "bcryptjs": "2.4.3",
    "cors": "2.8.5",
    "dotenv": "16.3.1",
    "express": "4.18.2",
    "express-validator": "7.2.1",
    "jsonwebtoken": "9.0.2",
    "mongoose": "8.0.3",
    "prisma": "^5.4.0",
    "serverless-http": "^3.2.0",
    "socket.io": "4.7.4",
    "stripe": "14.12.0",
    "uuid": "9.0.1",
    "validator": "13.11.0"
  }
}
